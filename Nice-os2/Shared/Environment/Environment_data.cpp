// Окружение приложения.
typedef struct _ENVIRONMENT
 {
  // Внутренние переменные.
  typedef struct _RTSTS
   {
    // Окно рабочего стола.
    HWND Detected_Desktop_window;
   }
  RTSETTINGS; RTSETTINGS RTSettings;
 }
ENVIRONMENT; ENVIRONMENT Environment;

